# multi-layer-demo

A tiny demo for the chatbot-sandbox workload-decomposition case. Three layers
share this repo: a backend router in `app.py`, a page in `index.html`, and
this README. The task touches all three.

## Endpoints

- `GET /` — serves the HTML page.